def addStrings(num1, num2):
        # what data type would you like back?
        # since num1 and num2 are strings, we must convert them to integers to perform any type of math on them
        # because you want a string returned, we have to wrap the solution in str() to convert it back
        return str(int(num1) + int(num2))